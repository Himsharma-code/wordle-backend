const express = require('express');
const router = express.Router();
const Joi = require('joi');
const Book = require('../models/auth');
require('dotenv').config();

router.post('/login', async (req, res) =>{
  
});

module.exports = router;
